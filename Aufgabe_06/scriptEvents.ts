namespace Aufgabe_06 {
    let count: number = 0;
    let sum: number = 0;
    
    document.querySelector("").addEventListener("button", handleButton);
    document.querySelector("").addEventListener("button", preisRechner);

    function handleButton(_event: Event): void {
        count++;
        //console.log(_event);
    }

    function preisRechner(_event: Event): void {
        
        console.log(sum);
    }
}
